# Unraid Templates for docker-demo

XML templates for Unraid Community Applications.

## Install
Copy `my-docker-demo.xml` to `/boot/config/plugins/dockerMan/templates-user/` on Unraid,
then: Docker tab → Add Container → select from template list.
